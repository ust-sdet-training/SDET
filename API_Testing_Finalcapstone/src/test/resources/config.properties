base.url=http://localhost:8080
# Database connection used by the DB validation test
# These values are overridden by Testcontainers when available
# but remain usable for local manual runs.
db.url=jdbc:postgresql://localhost:5000/tripstack
db.user=postgres
db.password=
